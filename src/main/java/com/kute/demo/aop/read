1. 相比 aspectj属于动态织入
2. 动态下又分为 静, 动织入

静: 只是在第一次调用执行
动: 根据代码运行的中间状态动态执行,每次调用target都执行

3. 两种代理机制

JDK动态代理   CGLLib 动态代理

4. 只有在基于该对象是spring bean时才有效

5. @Order 定义了切面的执行优先级

在切入点前的操作，按order的值由小到大执行
在切入点后的操作，按order的值由大到小执行